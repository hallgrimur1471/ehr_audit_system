# Server

## Install

``` 
# First cd into the directory of this README.md, then:
python3 -m venv .venv
source .venv/bin/activate
python3 -m pip install -r requirements.txt
python3 -m pip install -e .
```